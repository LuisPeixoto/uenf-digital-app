import React, { useEffect } from 'react';
import Card from '../../components/assignment/cardAssignment'

function assignmentScreen({ navigation }) {
    {
        return <Card navigation={navigation}/>
    }
}

export default assignmentScreen;

